<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.egov.notice.service.NoticeMapper">
    <select id="selectNotices" resultType="org.example.egov.notice.service.NoticeResponse">
        SELECT NOTICE_ID, TITLE, CONTENTS, CREATED_AT
        FROM NOTICE
        ORDER BY NOTICE_ID DESC
    </select>

    <select id="selectNotice" parameterType="long" resultType="org.example.egov.notice.service.NoticeResponse">
        SELECT NOTICE_ID, TITLE, CONTENTS, CREATED_AT
        FROM NOTICE
        WHERE NOTICE_ID = #{noticeId}
    </select>

    <insert id="insertNotice" parameterType="org.example.egov.notice.service.CreateNoticeCommand" useGeneratedKeys="true" keyProperty="noticeId">
        INSERT INTO NOTICE (TITLE, CONTENTS, CREATED_AT)
        VALUES (#{title}, #{contents}, CURRENT_TIMESTAMP)
    </insert>
</mapper>
