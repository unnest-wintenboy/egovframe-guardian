# Security Policy

Report security issues privately to the plugin maintainer before public disclosure.

This plugin runs local hook commands. Review `hooks/hooks.json` and `scripts/egovframe_guard.py` before installing in a shared environment.

The guard scanner is static analysis for development assistance. It is not a replacement for dependency scanning, SAST, secret scanning, or manual security review.
